name: Unit Tests
on: [push]
jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macOS-latest]
    steps:
      - name: Set up Go 1.12
        uses: actions/setup-go@v1
        with:
          go-version: 1.12
        id: go

			- name: Install Task for Linux or macOS
				if: matrix.os != 'windows-latest'
				run: curl -sL https://taskfile.dev/install.sh | sh

			- name: Install Task for Windows
				if: matrix.os == 'windows-latest'
				run: |
					iwr -useb get.scoop.sh | iex
					scoop bucket add extras
					scoop install task

      - name: Check out code
        uses: actions/checkout@v1

      - name: Get dependencies
        run: task install

      - name: Run tests
        run: task test
